defmodule TrackAppWeb.SessionController do
  use TrackAppWeb, :controller

  def list(conn, _options) do
  end

  # Invite user to join your session
  def invite(conn, %{user_id: user_id}) do
  end

end
